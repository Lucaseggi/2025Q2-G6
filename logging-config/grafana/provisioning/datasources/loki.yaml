apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      maxLines: 10000
      derivedFields:
        - datasourceUid: loki
          matcherRegex: "infoleg_id[\":]\\s*(\\d+)"
          name: InfolegID
          url: $${__value.raw}
